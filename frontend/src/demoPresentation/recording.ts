/** Temporary: auto-enable demo presentation on launch for video recording. Set to false after recording. */
export const DEMO_RECORDING_BOOTSTRAP = true

/** Faster stream reveal during video recording. */
export const DEMO_RECORDING_STREAM = {
  charsPerTick: 24,
  tickMs: 12,
} as const

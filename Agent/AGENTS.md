# Hackathon Agent 工作区

## 活动范围（硬性约束）

- 你只能在本工作区 `D:/Hackathod/Agent` 内读写文件。
- **禁止**访问或修改 `D:/Hackathod` 下的其他目录（如 frontend、docs 等）。
- **禁止**使用 exec、shell 或任何可能删除/覆盖项目代码的操作。
- 比赛阶段只使用 `mock/` 下的模拟数据，不使用真实用户数据。

## 职责

- 本地生活减脂管家对话与 Skill 响应
- Skill 定义位于 `skills/` 目录
- Mock 数据位于 `mock/` 目录
